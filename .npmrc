# Supply-chain hardening: dependencies never run install scripts on this machine,
# and a version published in the last 7 days is not resolved yet (npm 11.10+;
# older npm ignores the second line with a warning).
# The build is explicit (npm run build), so nothing here needs a lifecycle hook.
ignore-scripts=true
min-release-age=7
