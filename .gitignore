# Secrets - never committed (none are needed to run this server, keep it that way)
.env
.env.*
*.pem
*.key
.dev.vars

# Build + deps
node_modules/
dist/

# Local caches / scratch
.cache/
*.log
.DS_Store
Thumbs.db
!.env.example
secrets.env
